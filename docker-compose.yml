version: "3.9"

services:
  backend:
    build: ./backend/
    command: uvicorn src.main:app
    ports:
      - 8000:8000

  frontend:
    build: ./frontend/
    network_mode: host
    depends_on:
      - backend